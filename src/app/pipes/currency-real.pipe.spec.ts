import { CurrencyRealPipe } from './currency-real.pipe';

describe('CurrencyRealPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyRealPipe();
    expect(pipe).toBeTruthy();
  });
});
